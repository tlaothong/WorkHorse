<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:TheS_Casinova_Colors="clr-namespace:TheS.Casinova.Colors" x:Class="TheS.Casinova.Colors.Controls.GameInformationsUI"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400" Width="640" Height="480">
    
	<UserControl.Resources>
		<TheS_Casinova_Colors:GameApplicationInformation x:Key="GameApplicationInformationDataSource" d:IsDataSource="True"/>
	</UserControl.Resources>
	<UserControl.DataContext>
		<Binding Source="{StaticResource GameApplicationInformationDataSource}"/>
	</UserControl.DataContext>
    
    <Grid x:Name="LayoutRoot">
    	<Rectangle Fill="#FFF4F4F5" Stroke="Black" d:IsLocked="True"/>
    	<TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="{Binding Name, Mode=OneWay}" VerticalAlignment="Bottom" Margin="8,0,0,200" FontWeight="Bold" FontSize="13.333"/>
    	<TextBlock TextWrapping="Wrap" Text="Color or colour (see spelling differences) is the visual perceptual property corresponding in humans to the categories called red, green, blue and others. Color derives from the spectrum of light (distribution of light energy versus wavelength) interacting in the eye with the spectral sensitivities of the light receptors. Color categories and physical specifications of color are also associated with objects, materials, light sources, etc., based on their physical properties such as light absorption, reflection, or emission spectra. By defining a color space, colors can be identified numerically by their coordinates." Margin="30,0,30,100" VerticalAlignment="Bottom"/>
    	<Rectangle Stroke="Black" Margin="8,8,8,223">
    		<Rectangle.Fill>
    			<RadialGradientBrush>
    				<GradientStop Color="#FFE24444" Offset="0.123"/>
    				<GradientStop Color="#FFFFAF00" Offset="1"/>
    			</RadialGradientBrush>
    		</Rectangle.Fill>
    	</Rectangle>
    	<HyperlinkButton Content="Play" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="176,0,0,60" Foreground="Red" NavigateUri="{Binding GameUri, Mode=OneWay}"/>
    	<Button Content="How to play" HorizontalAlignment="Right" Height="56" Margin="0,0,176,40" VerticalAlignment="Bottom" Width="104"/>
    </Grid>
</UserControl>

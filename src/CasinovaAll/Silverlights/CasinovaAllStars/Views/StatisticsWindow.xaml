<controls:ChildWindow
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
           xmlns:CasinovaAllStars_ViewModels="clr-namespace:CasinovaAllStars.ViewModels" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="CasinovaAllStars.Views.StatisticsWindow"
           Width="600" Height="400" 
           Title="StatisticsWindow" FontSize="21.333">
	<controls:ChildWindow.Resources>
		<CasinovaAllStars_ViewModels:StatisticsViewModel x:Key="StatisticsModelDataSource" d:IsDataSource="True"/>
		<DataTemplate x:Key="DataTemplate">
			<StackPanel>
				<TextBlock Text="{Binding Metadata.GameName, Mode=OneWay}"/>
			</StackPanel>
		</DataTemplate>
	</controls:ChildWindow.Resources>
	<controls:ChildWindow.DataContext>
		<Binding Source="{StaticResource StatisticsModelDataSource}"/>
	</controls:ChildWindow.DataContext>
    <Grid x:Name="LayoutRoot" Margin="2">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Button x:Name="CloseButton" Content="Close" Click="CloseButton_Click" Width="75" Height="23" HorizontalAlignment="Right" Margin="0,12,0,0" Grid.Row="1" FontSize="11" />
        <Grid DataContext="{Binding SelectedItem, ElementName=GameRoomListBox}" Margin="0,40,0,0" >
        	<ContentPresenter Content="{Binding Value, Mode=OneWay}"/>
        </Grid>
        <ListBox x:Name="SelectedItem" ItemTemplate="{StaticResource DataTemplate}" ItemsSource="{Binding StatContents}" Height="36" VerticalAlignment="Top" Background="{x:Null}"/>
    </Grid>
</controls:ChildWindow>